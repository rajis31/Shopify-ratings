# App Features
1. Allow logged in users to leave a review on the orders tab
2. Allows shop owners to track reviews on the admin side 
  - Show # of reviews
  - Show all reviews (paginated) on a table using 
     - `review.id`
     - `review.user.email`
     - `review.review`
     - `review.created_at`

  